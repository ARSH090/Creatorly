[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npx tsx src/workers/main.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production.variables]
NODE_ENV = "production"
